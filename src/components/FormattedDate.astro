---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-muted font-thin" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
